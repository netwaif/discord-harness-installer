# discord-harness-installer 시작 브리프

컨트롤타워 세션(2026-08-03)이 넘기는 v3 착수 브리프. 이 폴더의 새 세션이 맨몸으로
시작하지 않도록, 확정된 결정·정본 경로·연계 요청을 모아 둔다.

## 목표 (2026-08-02 사용자 확정 — 컨트롤타워 SESSION.md 결정 기록 원문 기준)

**통합 설치기 플러그인**: configure-harness 스킬 + botctl급 결정적 엔진으로
하네스·브리지·대시보드 설치 전 과정을 플러그인 하나에 담는다.

- 범위: 레포 구성 · 토큰 `.bot-token` 파일 수령(채팅 수신 금지) · LaunchAgent ·
  자동 검증 · doctor · 설치→제거 diff 0
- 디스코드 포탈 수동 단계(서버·봇 앱 4개·토큰·초대)는 기술적으로 제거 불가 —
  folder-bot식 단계 안내가 상한
- 훅: "매뉴얼 16장이 스킬 하나가 됐습니다"
- 실증 근거: loadout · multi-agent-starter · folder-bot(botctl) 3회 반복된 패턴 +
  코덱스 마켓플레이스 호환 E2E 성공(2026-08-02 — `codex plugin marketplace add`가
  클로드 형식 marketplace.json을 그대로 소비)
- 진단: 현 매뉴얼의 "AI 지침 붙여넣기" 구조는 과도기 산물(모델 즉흥 구현 편차가 약점)
- 규모 감: 엔진 확장·스킬·4봇 E2E 며칠

## 설치 대상 3레포 (정본)

| 대상 | 로컬 | 원격 |
|---|---|---|
| 하네스 본체 | `~/ai-folder/dev/discord-multiagent` | github.com/netwaif/discord-multiagent (작업 브랜치 release-snapshot → main refspec 주의 — main 로컬 브랜치 없음) |
| 수다 브리지 | `~/ai-folder/dev/codex-discord` | github.com/netwaif/codex-discord |
| 대시보드 | `~/VSCodeWorkspace/usage-coach` | github.com/netwaif/usage-coach |

## 패턴 참조 (그대로 승계할 것)

- **folder-bot** = 구조의 원형: `~/VSCodeWorkspace/folder-bot`
  (`.claude-plugin/marketplace.json` / `plugins/<이름>/skills/<스킬>/generator/botctl.py` /
  assets / tests 17개 / 설치→제거 diff 0 테스트). 스펙·플랜 전례 =
  하네스 레포 `docs/superpowers/{specs,plans}/2026-08-01-*`
- 봇 기동 직렬화 = 하네스 `scripts/bot-up.sh`(mkdir 원자 락) / 원격 재시작 =
  `scripts/bot-restart.sh` — 설치기가 깔아주는 최종 형태의 정본
- 매뉴얼 정본(현행 v2.2, 16장·38쪽) = `~/VSCodeWorkspace/discord-multiagent-manual/`
  — 설치기가 대체할 대상이자 검증 기준

## 연계 요청 1건 (공개 답변으로 약속됨 — 2026-08-03)

멤버 긱님(geek7942) 제안: multi-agent-starter의 **사용자 워커 custom registry**.
update가 `_shared/backends.json`을 재생성해 사용자 추가 워커가 지워지는 문제 →
원본 목록 + 사용자 목록(_local, update 보존) 병합 구조 요청.
"통합 설치기 작업에 함께 검토하겠다"고 공개 답변함(약속 수위: 검토, 시점 미약속).
- multi-agent-starter 정본 = `~/VSCodeWorkspace/MultiAgent` (v3.5.0,
  github.com/netwaif/multi-agent-starter)
- 컨트롤타워 판단(2026-08-03): 1단계(등록부 병합)만 우선, 라우팅 문서·디스코드
  이름표 확장은 보류 권고. 스키마 변경 시 사용자 조각 호환성 방어 필요.

## 절차 권고

folder-bot 전례대로: 브레인스토밍 → 스펙 → 플랜 → 구현 → 4봇 E2E.
첫 세션은 구현에 들어가지 말고 스펙까지만.
